#!/bin/sh
# execute freetts with the correct classpath
TOP=`dirname $0`/../
LIBDIR="$TOP/lib"
JARS="$LIBDIR/freetts.jar"
if [ -z "${JAVA_HOME}" ] ; then
    JAVA_HOME=/lab/speech/java/j2sdk1.4.0
fi
${JAVA_HOME}/bin/java -Xms64m -ea -cp "$JARS:$TOP/demo/classes" \
	-Dmbrola.base=${MBROLA_DIR} \
	$@
