BASE=../
FREETTS_CLASSES=$BASE/classes
if [ -z "${JAVA_HOME}" ] ; then
    JAVA_HOME=/lab/speech/java/j2sdk1.4.0
fi
${JAVA_HOME}/bin/java -server -Xms64m -ea -cp $FREETTS_CLASSES com.sun.speech.freetts.FreeTTS  -silent -run "server $*" -metrics -lines $*
